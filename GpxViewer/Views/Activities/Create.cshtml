@model GpxViewer.ViewModels.CreateActivityViewModel

@using (Html.BeginForm("Create", "Activities", FormMethod.Post, new { @enctype = "multipart/form-data", @class = "form-horizontal" }))
{
    <div class="page-header color">
        <h1>Upload GPX File</h1>
    </div>
    @Html.AntiForgeryToken()
    @*<div class="control-group">
        @Html.LabelFor(model => model.Track.Name, new { @class = "control-label" })
        <div class="controls">
            @Html.TextBoxFor(m => m.Track.Name, new { @class = "text_field" })
            @Html.ValidationMessageFor(m => m.Track.Name)
        </div>
    </div>*@
    <div class="control-group">
        @Html.LabelFor(model => model.GpxFile, new { @class = "control-label" })
        <div class="controls">
            <input type="file" name="GpxFile" />
             @Html.ValidationMessageFor(x => x.GpxFile)
        </div>
    </div>
    <div class="form-actions">
        <input type="submit" value="Upload" class="btn btn-primary" />
        @Html.ActionLink("Cancel", "Index", "Activities", null, new { @class = "btn" })

    </div>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
